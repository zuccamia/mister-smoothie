<div class='container mt-5'>
  <%= simple_form_for @ingredient do |f| %>
    <%= f.input :name %>
    <%= f.input :img_url, label: "Image URL" %>
    <%= f.submit "Add ingredient", class: "btn btn-rounded btn-info" %>
  <% end %>
</div>

